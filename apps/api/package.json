{
  "name": "@cross-pr-risk-analyzer/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "type-check": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests --exclude \"**/*.smoke.spec.ts\"",
    "test:claude-smoke": "vitest run src/risk-analysis/claude/claude-risk-analysis-provider.smoke.spec.ts",
    "lint": "eslint src",
    "start": "node dist/main.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "~6.0.2",
    "vitest": "^4.0.8"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.117.1",
    "diff": "^9.0.0",
    "fastify": "^5.2.0",
    "tree-sitter": "^0.21.1",
    "tree-sitter-typescript": "^0.23.2",
    "zod": "^4.4.3"
  }
}
